import { memo, useEffect } from "react"

const TableRow = ({ row, customFn }) => {
    // useEffect(() => {
        // customFn()
        console.log(1)
    // })

    return (
       <div></div>
    )
}

export default memo(TableRow)